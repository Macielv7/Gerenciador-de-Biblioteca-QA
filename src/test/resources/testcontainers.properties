# Testcontainers - reutilização desabilitada para garantir isolamento entre testes
testcontainers.reuse.enable=false
# Força o uso do socket Docker Desktop no Windows
# Este valor garante que o processo Java consiga conectar ao mesmo endpoint usado pelo Docker CLI.
docker.host=npipe:////./pipe/docker_engine
docker.tls.verify=false
